void main() {
  // Ternary Operator
  /// Ternary operator adalah operator yang memungkinkan kita untuk menulis kode if else dalam satu baris.
  int nilai = 80;
  String hasil = nilai >= 75 ? 'Selamat, Anda Lulus!' : 'Anda Tidak Lulus!';
  print(hasil);
}